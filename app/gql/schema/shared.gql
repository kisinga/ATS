type Query
type Mutation
type Subscription

interface BaseObject {
  ID: ID!
  updatedBy: User
  createdBy: User
}

# Information for paginating this connection
type PageInfo {
  startCursor: ID!
  endCursor: ID!
  hasNextPage: Boolean!
  count: Int!
}
